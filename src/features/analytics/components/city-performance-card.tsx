import { CardTitle } from "@/features/analytics/components/card-title";
import type { CityPoint } from "@/features/analytics/types/analytics";
const nf = new Intl.NumberFormat("ar-SA");
export function CityPerformanceCard({ cities }: { cities: CityPoint[] }) { const maximum = cities[0]?.value || 1; return <section aria-labelledby="cities-title" className="dashboard-card"><CardTitle id="cities-title" title="أعلى المدن أداءً" subtitle="عدد الرحلات المنفذة" /><ol className="space-y-4 p-4">{cities.map((city) => <li key={city.city} className="grid grid-cols-[58px_1fr_auto] items-center gap-2 text-[10px]"><span className="font-semibold">{city.city}</span><span className="h-2 overflow-hidden rounded bg-muted"><i className="block h-full rounded bg-primary" style={{ width: `${city.value / maximum * 100}%` }} /></span><bdi dir="ltr" className="w-12 text-left font-bold text-muted-foreground">{nf.format(city.value)}</bdi></li>)}</ol></section>; }

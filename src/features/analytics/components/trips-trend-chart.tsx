"use client";
import { Area, AreaChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { CardTitle } from "@/features/analytics/components/card-title";
import type { TrendPoint } from "@/features/analytics/types/analytics";
import { numberFormatter as nf } from "@/lib/formatters";
export function TripsTrendChart({ data, periodLabel, total, primarySeriesLabel, secondarySeriesLabel }: { data: TrendPoint[]; periodLabel: string; total: number; primarySeriesLabel: string; secondarySeriesLabel: string }) {
  return <section aria-labelledby="trend-title" className="dashboard-card overflow-hidden xl:h-full"><CardTitle id="trend-title" title="الرحلات والطلبات عبر الوقت" subtitle={periodLabel} /><p className="sr-only">إجمالي النقاط المعروضة {nf.format(total)} رحلة. المساحة النيلية تمثل {primarySeriesLabel}، والزرقاء تمثل {secondarySeriesLabel}.</p><div dir="ltr" role="img" aria-label={`اتجاه ${nf.format(total)} رحلة خلال الفترة المختارة`} className="h-[275px] px-2 pb-3 pt-3"><ResponsiveContainer><AreaChart data={data}><defs><linearGradient id="trips-area-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#1F0F8C" stopOpacity={0.18} /><stop offset="95%" stopColor="#1F0F8C" stopOpacity={0.02} /></linearGradient><linearGradient id="completed-area-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#376FF1" stopOpacity={0.2} /><stop offset="95%" stopColor="#376FF1" stopOpacity={0.02} /></linearGradient></defs><CartesianGrid stroke="#eceef2" vertical={false} /><XAxis dataKey="label" tick={{ fontSize: 10 }} axisLine={false} tickLine={false} /><YAxis tick={{ fontSize: 10 }} axisLine={false} tickLine={false} /><Tooltip formatter={(value) => nf.format(Number(value))} /><Legend wrapperStyle={{ fontSize: 10, direction: "rtl" }} /><Area type="monotone" dataKey="trips" name={primarySeriesLabel} stroke="#1F0F8C" fill="url(#trips-area-fill)" strokeWidth={2.5} dot={false} activeDot={{ r: 3 }} isAnimationActive={false} /><Area type="monotone" dataKey="completed" name={secondarySeriesLabel} stroke="#376FF1" fill="url(#completed-area-fill)" strokeWidth={2} dot={false} activeDot={{ r: 3 }} isAnimationActive={false} /></AreaChart></ResponsiveContainer></div></section>;
}
